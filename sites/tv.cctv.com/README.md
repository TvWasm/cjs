# tv.cctv.com

This directory owns its JS and native library. See `site.json` for the version, module
name and quality tiers, and [protocol](../../docs/plugin-protocol.md) for the host contract.
Build with `tools/build-native.ps1 -Site tv.cctv.com`; increment this site version before
publishing changed scripts or native artifacts.
